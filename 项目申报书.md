# MoonBit AccessLog 项目申报书

## 基本信息

- 项目名称：MoonBit AccessLog
- 项目方向：MoonBit 日志解析、访问统计与归档工具
- 参赛者：pei0331
- GitHub：https://github.com/pei0331/moon-accesslog
- GitLink：https://gitlink.org.cn/pei0331/moon-accesslog
- 主要语言：MoonBit
- 当前版本：0.1.0
- 开源许可证：Apache License 2.0

## 项目简介

MoonBit AccessLog 是一个无第三方运行时依赖的 Nginx/Apache Common Log
Format 与 Combined Log Format 分析器。项目使用 MoonBit 从零实现日志行解析、
容错处理、聚合统计和命令行报告输出，适合对访问日志进行快速检查、归档和
CI 数据采集。

工具在遇到格式错误的记录时会计数并跳过，不影响其余有效记录的统计。报告
同时提供人类可读文本、确定性 JSON 和标准 CSV 三种输出形式。

## 核心功能

- 解析 Common/Combined Log Format，正确处理请求、引用页和 User-Agent 引号字段。
- 聚合请求总数、无效行数、传输字节、HTTP 状态码、请求方法、路径、客户端地址和 UTC 小时。
- 提供错误请求数、错误率和唯一路径数等健康指标。
- `Report::to_text()` 输出终端友好的摘要。
- `Report::to_json()` 输出稳定排序的机器可读 JSON。
- `Report::to_csv()` 输出 `metric,key,value` 归档表格，并执行 CSV 字段转义。
- CLI 支持 `--top N`、`--json` 和 `--csv`，适合本地分析与自动化任务。
- 仅依赖 `moonbitlang/core`，不访问网络、不依赖服务端运行时。

## 使用方式

```bash
moon run cmd/main -- examples/access.log
moon run cmd/main -- --top 5 examples/access.log
moon run cmd/main -- --json examples/access.log
moon run cmd/main -- --csv examples/access.log
```

库调用示例：

```moonbit
let report = @accesslog.analyze(source)
println(report.to_text(top=10))
let json = report.to_json()
let csv = report.to_csv()
```

## 本期完成工作

本期在已有解析器基础上完成了可交付的报告输出与运维指标能力：

1. 新增确定性 JSON 报告输出和 CLI `--json` 模式。
2. 新增错误请求数、错误率、唯一路径数，并同步到文本和 JSON 报告。
3. 新增标准 CSV 导出和 CLI `--csv` 模式，支持字段转义与稳定排序。
4. 增加对应单元测试、README 使用说明和 CHANGELOG。

本期主要提交记录：

- `ac6f1ea`：新增 JSON 报告输出。
- `4ecf477`：补充 JSON 工作流和参考来源文档。
- `5e0f659`：新增错误和路径指标。
- `c6c3840`：补充指标说明。
- `ed01b40`：新增 CSV 报告导出。
- `9e3b68f`：补充 CSV 工作流文档。

## 原创与参考来源

解析器、聚合器、报告渲染器、CLI 和测试均为本项目的 MoonBit 原创实现，
未复制或移植 Nginx、Apache 的源代码。语法和字段语义参考：

- Nginx `log_format`：<https://nginx.org/en/docs/http/ngx_http_log_module.html>
- Apache `mod_log_config`：<https://httpd.apache.org/docs/2.4/mod/mod_log_config.html>

参考项目和格式文档不作为运行时依赖，详细说明见 [THIRD_PARTY.md](THIRD_PARTY.md)。

## 验证与可运行性

项目提供 `examples/access.log`、CLI 示例和库级测试。当前验证命令：

```bash
moon fmt --check
moon info
moon check
moon test
moon build --target native
```

当前测试结果：6 项测试全部通过，native 构建成功。CI 配置位于
`.github/workflows/ci.yml`，用于持续执行格式化、检查、测试和构建。

## GitLink 同步

项目 GitLink 镜像地址为：
<https://gitlink.org.cn/pei0331/moon-accesslog>。

GitHub 与 GitLink 均使用 `main` 分支。同步命令为：

```bash
git remote add gitlink https://gitlink.org.cn/pei0331/moon-accesslog
git push -u gitlink main
```

## 开源许可证

项目使用 Apache License 2.0，许可证全文见 [LICENSE](LICENSE)。

## AI 辅助说明

项目开发过程中使用 AI 辅助进行代码审阅、测试设计、文档整理和命令检查。
项目目标、模块边界、统计口径、输出格式、提交内容和验收结果由参赛者确定，
所有生成代码均经过 MoonBit 格式化、类型检查、测试和 native 构建验证。

## 后续计划

- 支持按时间窗口和状态码过滤日志。
- 为超大文件增加流式读取模式，降低内存占用。
- 增加更多服务器日志格式的兼容性测试。
- 继续保持 GitHub、GitLink 和 CI 的提交记录同步。
