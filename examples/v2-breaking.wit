package demo:ledger;

interface account {
  record user {
    id: u64,
    display-name: string,
  }

  get-user: func(id: u64) -> user;
}

world service {
  import log: func(message: string);
  export account;
  export health: func() -> bool;
}
